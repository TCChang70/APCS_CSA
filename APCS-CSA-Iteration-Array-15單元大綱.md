# APCS CSA — Iteration & Array 15 單元詳細大綱

> **語言：** Java｜**涵蓋：** Unit 4 Iteration + Unit 6 Array  
> **總單元：** 15 個｜**每單元：** 2 小時｜**總計：** 30 小時

---

## 大綱導覽

| # | 單元標題 | 階段 | 難度 | 核心概念 |
|---|---------|------|------|---------|
| 01 | `while` 迴圈基礎 | Phase 1 | ⭐ | Entry Condition、無限迴圈 |
| 02 | `for` 迴圈基礎 | Phase 1 | ⭐ | 三段結構、遞增/遞減/步進 |
| 03 | 迴圈控制變數與條件設計 | Phase 1 | ⭐⭐ | Off-by-one Error、次數公式 |
| 04 | 巢狀迴圈 | Phase 1 | ⭐⭐⭐ | Nested Loops、二維圖形 |
| 05 | String 字串遍歷 | Phase 1 | ⭐⭐ | charAt()、length()、字元操作 |
| 06 | 迴圈與數學計算 | Phase 2 | ⭐⭐ | Running Total、階乘、GCD |
| 07 | break 與 continue | Phase 2 | ⭐⭐⭐ | 跳出迴圈、跳過迭代 |
| 08 | 常見迴圈錯誤與除錯技巧 | Phase 2 | ⭐⭐⭐ | 5 大 Bug 類型、Debug 方法 |
| 09 | 迴圈與方法整合 | Phase 2 | ⭐⭐⭐ | 方法封裝、回傳值 |
| 10 | Iteration 綜合練習與 FRQ 準備 | Phase 2 | ⭐⭐⭐⭐ | FRQ 格式、密碼驗證 |
| 11 | Array 宣告與初始化 | Phase 3 | ⭐ | 三種初始化、預設值、length |
| 12 | Array 元素存取與修改 | Phase 3 | ⭐⭐ | 參考型別、就地修改 |
| 13 | Array 遍歷 — 標準 `for` | Phase 3 | ⭐⭐ | 累加/計數/搜尋/最大值 |
| 14 | Array 遍歷 — `for-each` | Phase 3 | ⭐⭐ | 增強式迴圈、使用限制 |
| 15 | Array 基礎演算法 | Phase 3 | ⭐⭐⭐ | 最大/最小/總和/線性搜尋 |

---

## Phase 1 — Iteration 基礎（Unit 01–05）

---

### Unit 01：`while` 迴圈基礎

**預估時間：** 2 小時｜**難度：** ⭐☆☆☆☆

#### 大綱

1. **概念導入**
   - `while` 迴圈定義：Entry Condition（進入條件）
   - 執行流程圖：檢查條件 → 執行主體 → 回到條件
   - 與「重複做某件事直到條件不成立」的日常對應

2. **語法結構**
   ```
   while (條件) { 主體 }
   ```
   - 條件（boolean expression）
   - 迴圈主體（loop body）
   - 更新語句（update statement）的必要性

3. **程式碼範例（3 個）**
   - 從 1 計數到 5（基本計數）
   - 計算 1 到 10 的總和（累加模式）
   - 讀取輸入直到輸入 0（哨兵值模式，Sentinel Pattern）

4. **練習題**
   - Easy：倒數計時從 10 到 1，最後印 "Go!"
   - Medium：找出第一個大於 50 且能被 7 整除的數

5. **常見錯誤表格**
   - 無限迴圈（忘記更新變數）
   - Off-by-one（`<` vs `<=`）
   - 賦值 vs 比較（`=` vs `==`）

6. **現在試試看**
   - 計算 1 到 100 中所有奇數的總和

---

### Unit 02：`for` 迴圈基礎

**預估時間：** 2 小時｜**難度：** ⭐☆☆☆☆

#### 大綱

1. **概念導入**
   - `for` 迴圈適用場景：次數已知的重複
   - 三段結構：初始化、條件、更新
   - 為何比 `while` 更適合計數型迴圈

2. **語法結構**
   ```
   for (初始化; 條件; 更新) { 主體 }
   ```
   - 各段的執行時機（初始化只執行一次）
   - 變數作用域（scope）限制在 for 區塊內

3. **計數方向變化**
   - 遞增（`i++`、`i += n`）
   - 遞減（`i--`、`i -= n`）
   - 步進（`i += 2`、`i *= 2`）

4. **`for` vs `while` 對照表**
   - 等價轉換範例
   - 各自適用時機

5. **程式碼範例（4 個）**
   - 基本遞增計數
   - 遞減計數
   - 步進 2（印偶數）
   - 乘法表單行

6. **練習題**
   - Easy：印出 1–20 的偶數
   - Medium：使用巢狀 for 印出星號三角形

7. **常見錯誤**
   - 分隔符號錯誤（逗號 vs 分號）
   - 迴圈內再次宣告控制變數

8. **現在試試看**
   - 印出 1–100 中能被 3 整除的數，統計個數

---

### Unit 03：迴圈控制變數與條件設計

**預估時間：** 2 小時｜**難度：** ⭐⭐☆☆☆

#### 大綱

1. **概念導入**
   - 控制變數（loop control variable）的角色
   - 精確設定起始值與終止條件的重要性
   - APCS 考試中最常出現的陷阱題型

2. **Off-by-one Error 詳解**
   - `<` 與 `<=` 的差別
   - 計算公式：`i <= b` 執行 `b - a + 1` 次；`i < b` 執行 `b - a` 次
   - 索引從 0 開始的常見誤解

3. **條件設計技巧**
   - 複合條件（`&&`、`||`）在迴圈中的應用
   - 使用方法回傳值作為條件（`s.length()`）
   - 浮點數條件的陷阱（精度問題）

4. **程式碼範例（3 個）**
   - 精確控制範圍（索引 2 到 7）
   - 計算 1 到 100 總和（確認邊界）
   - String 長度作為終止條件

5. **練習題**
   - Easy：紙筆計算四個迴圈的執行次數
   - Medium：FizzBuzz（1–30，條件順序的重要性）

6. **常見錯誤**
   - 在迴圈內意外修改控制變數
   - 浮點數比較導致無限迴圈

7. **現在試試看**
   - 計算 1 到 50 中所有質數的個數

---

### Unit 04：巢狀迴圈（Nested Loops）

**預估時間：** 2 小時｜**難度：** ⭐⭐⭐☆☆

#### 大綱

1. **概念導入**
   - 巢狀迴圈定義：迴圈中的迴圈
   - 執行順序：外層一次，內層完整一圈
   - 總執行次數 = 外層次數 × 內層次數

2. **執行追蹤方法**
   - 用表格追蹤 `(i, j)` 的每個組合
   - 識別內外層變數的獨立性
   - APCS MCQ 常考：追蹤 `count` 最終值

3. **應用場景**
   - 印出二維圖形（矩形、三角形、菱形）
   - 生成乘法表
   - 巢狀搜尋（比較所有元素對）

4. **程式碼範例（3 個）**
   - 追蹤 i、j 組合（輸出 9 行）
   - 乘法表（9×9）
   - 矩形圖案（4 行 6 列）

5. **練習題**
   - Easy：追蹤 `count` 最終值（三角形執行次數）
   - Medium：印出數字三角形（每行印 1 到 i）
   - Hard：用巢狀迴圈找質數（2–50）

6. **常見錯誤**
   - 內外層變數同名（遮蔽問題）
   - 誤解執行順序（以為外層先跑完）
   - 忽略效能（三層巢狀迴圈的複雜度）

7. **現在試試看**
   - 印出完整九九乘法表（格式化輸出）

---

### Unit 05：String 字串遍歷（String Traversal）

**預估時間：** 2 小時｜**難度：** ⭐⭐☆☆☆

#### 大綱

1. **概念導入**
   - 字串（String）的索引結構（0-based）
   - 字串遍歷（String traversal）的概念
   - APCS 中字串遍歷的重要性

2. **關鍵方法**

   | 方法 | 說明 | 範例 |
   |------|------|------|
   | `s.length()` | 回傳字串長度 | `"Java".length()` → 4 |
   | `s.charAt(i)` | 回傳索引 i 的字元（`char`）| `"Java".charAt(0)` → `'J'` |
   | `s.indexOf(c)` | 回傳字元位置（找不到回傳 -1）| `"banana".indexOf('a')` → 1 |
   | `s.substring(a,b)` | 回傳子字串（a 到 b-1）| `"Hello".substring(1,3)` → `"el"` |

3. **遍歷模式（4 種）**
   - 逐一印出每個字元
   - 統計特定字元出現次數
   - 建構反轉字串
   - 回文（Palindrome）判斷

4. **程式碼範例（4 個）**
   - 印出每個字元
   - 統計 'a' 出現次數
   - 反轉字串
   - 回文判斷（對稱索引比較）

5. **練習題**
   - Easy：統計字串中的大寫字母數
   - Medium：移除字串中的所有母音

6. **常見錯誤**
   - `s.length()` vs 陣列 `arr.length`（有無括號）
   - 索引越界（`charAt(s.length())`）
   - 誤用 `==` 比較 `String`（應比較 `char`）

7. **現在試試看**
   - 撰寫忽略大小寫、只考慮字母的回文判斷方法


## Phase 2 — Iteration 進階（Unit 06–10）

### Unit 06：迴圈與數學計算

**預估時間：** 2 小時｜**難度：** ⭐⭐☆☆☆

#### 大綱

1. **概念導入**
   - 迴圈是數學演算法的核心工具
   - 三大模式：累加（Running Total）、累乘（Running Product）、平均

2. **累加模式（Accumulator Pattern）**
   - 初始化累加器為 0
   - `sum += value` 模式
   - 應用：總和、條件總和

3. **累乘模式（Product Pattern）**
   - 初始化為 1（不能為 0！）
   - `product *= value` 模式
   - 應用：階乘（Factorial）、次方計算

4. **其他數學應用**
   - 平均值計算（注意 int/double 轉型）
   - 最大公因數（GCD）— Euclidean Algorithm with while
   - Fibonacci 數列（追蹤前兩值）

5. **程式碼範例（4 個）**
   - 計算 1–100 總和
   - 階乘計算
   - 陣列平均值（搭配 array）
   - GCD（輾轉相除法）

6. **練習題**
   - Easy：計算 `base^exp`（不使用 `Math.pow()`）
   - Medium：印出 Fibonacci 前 15 項

7. **現在試試看**
   - 計算 1² + 2² + ... + 20² 的總和

---

### Unit 07：`break` 與 `continue`

**預估時間：** 2 小時｜**難度：** ⭐⭐⭐☆☆

#### 大綱

1. **概念導入**
   - `break`：立即離開整個迴圈
   - `continue`：跳過本次迭代，繼續下一次
   - 兩者的使用時機與適當場景

2. **`break` 詳解**
   - 語法與執行流程
   - 應用場景：找到第一個目標後停止
   - 在巢狀迴圈中的行為（只跳出最內層）

3. **`continue` 詳解**
   - 語法與執行流程
   - 應用場景：過濾不需要處理的元素
   - 在 `for` 與 `while` 中的行為差異

4. **`break` vs `continue` vs `return` 比較表**

   | 關鍵字 | 跳出範圍 | 常見用途 |
   |--------|---------|---------|
   | `break` | 目前迴圈 | 搜尋找到後停止 |
   | `continue` | 本次迭代 | 跳過不符條件的元素 |
   | `return` | 整個方法 | 找到後直接回傳 |

5. **程式碼範例（3 個）**
   - 搜尋第一個負數（break）
   - 印出非空白字元（continue）
   - break 在巢狀迴圈（只跳出內層示範）

6. **練習題**
   - Easy：找 1–1000 中第一個大於 100 且被 13 整除的數
   - Hard：印出字串中每個字元的第一次出現位置

7. **現在試試看**
   - 改進質數判斷程式，找到因數後立即用 break 停止

---

### Unit 08：常見迴圈錯誤與除錯技巧

**預估時間：** 2 小時｜**難度：** ⭐⭐⭐☆☆

#### 大綱

1. **概念導入**
   - 為何迴圈是最常出 Bug 的地方
   - 系統化除錯的重要性
   - APCS 考試中的迴圈陷阱題型整理

2. **5 大常見錯誤**

   | # | 錯誤類型 | 描述 | 修正方法 |
   |---|---------|------|---------|
   | 1 | Off-by-one | 多一次或少一次 | 確認 `<` vs `<=` |
   | 2 | 無限迴圈 | 條件永不為 false | 確認更新邏輯 |
   | 3 | 初始化錯誤 | 累加從非 0 / 累乘從非 1 開始 | 根據操作選擇初始值 |
   | 4 | 作用域問題 | 迴圈外使用 for 的控制變數 | 在迴圈外宣告 |
   | 5 | 意外覆蓋 | 在迴圈內重複宣告累計變數 | 移到迴圈外宣告 |

3. **除錯技巧（4 種）**
   - Print debugging：在關鍵位置插入 `System.out.println()`
   - 手動追蹤（Trace Table）：用紙筆列出每次迭代的變數值
   - 邊界測試：測試 n=0、n=1、n=max 的情況
   - 二分法定位：縮小 print 語句範圍找到 bug 位置

4. **程式碼範例（錯誤版 + 修正版）**
   - Off-by-one（1 到 n 總和）
   - 累乘初始化錯誤（階乘結果為 0）
   - 無限迴圈（忘記 `i++`）
   - 作用域錯誤（for 外使用 `j`）

5. **練習題：找 Bug**
   - 分析一段有兩個 Bug 的程式碼，找出並修正

6. **APCS 考試陷阱題整理**
   - 追蹤含 bug 程式的輸出
   - 「以下哪個程式碼片段輸出相同結果？」

7. **現在試試看**
   - 撰寫計算方法，加入 print debug 觀察執行過程

---

### Unit 09：迴圈與方法（Methods）整合

**預估時間：** 2 小時｜**難度：** ⭐⭐⭐☆☆

#### 大綱

1. **概念導入**
   - 為何要將迴圈邏輯封裝進方法
   - 方法的可重用性（Reusability）
   - APCS FRQ 中「呼叫前一部分方法」的題型

2. **方法設計原則**
   - 一個方法做一件事（Single Responsibility）
   - 有意義的方法名稱（動詞 + 名詞）
   - 參數設計：哪些值應作為參數

3. **在迴圈中呼叫方法**
   - 將方法呼叫放入迴圈條件或主體
   - 分析每次呼叫的時間成本
   - 避免在迴圈條件中做不必要的重複計算

4. **回傳值與迴圈結合**
   - 迴圈計算後 return 結果
   - 提早 return 代替 break（在方法中）
   - boolean 方法的典型模式

5. **程式碼範例（3 個）**
   - `sum(int n)` 封裝累加邏輯
   - `isPrime(int n)` 封裝質數判斷（在迴圈中呼叫）
   - `countVowels(String s)` 封裝字串統計

6. **練習題**
   - Medium：實作 `max(int a, int b, int c)` 不使用 `Math.max()`
   - Hard：實作整數反轉方法 `reverse(int n)`

7. **FRQ 技巧：Part (a) → Part (b) 的呼叫模式**
   - Part (b) 通常可呼叫 Part (a) 方法
   - 範例：`average()` 被 `aboveAverage()` 呼叫

8. **現在試試看**
   - 撰寫 `sumOfSquares(int n)` 計算 1² + 2² + ... + n²

---

### Unit 10：Iteration 綜合練習與 FRQ 準備

**預估時間：** 2 小時｜**難度：** ⭐⭐⭐⭐☆

#### 大綱

1. **Unit 01–09 快速複習**
   - `while` vs `for` 選擇原則
   - 迴圈次數計算公式複習
   - break / continue / return 三者比較
   - 5 大常見錯誤清單回顧

2. **APCS CSA FRQ 迴圈題型分類**

   | 題型 | 頻率 | 關鍵技巧 |
   |------|------|---------|
   | 累加/計數 | ⭐⭐⭐⭐⭐ | 初始化在迴圈外 |
   | 搜尋（找第一個）| ⭐⭐⭐⭐ | break 或提早 return |
   | 字串逐字元處理 | ⭐⭐⭐⭐ | charAt() + length() |
   | 巢狀迴圈比對 | ⭐⭐⭐ | 內外層索引分工 |
   | 數學計算（GCD、次方）| ⭐⭐⭐ | 累乘、輾轉相除 |

3. **FRQ 答題格式規範**
   - 存取修飾詞（`public static`）
   - 回傳型別（`int`、`boolean`、`String`）
   - 參數命名（有意義）
   - 不遺漏 `return` 語句

4. **完整 FRQ 練習：密碼驗證方法**
   - `isValidPassword(String password)`
   - 條件：長度 ≥ 8、含大寫字母、含數字
   - 完整解答 + 評分標準說明

5. **Phase 1 & 2 里程碑自我檢查清單**
   - 8 個核心能力打勾確認

6. **現在試試看**
   - 撰寫方法判斷一個字串是否為「強密碼」（額外要求含特殊符號）


## Phase 3 — Array 基礎（Unit 11–15）

### Unit 11：Array 宣告與初始化

**預估時間：** 2 小時｜**難度：** ⭐☆☆☆☆

#### 大綱

1. **概念導入**
   - 陣列（Array）的定義：相同型別、固定大小、連續記憶體
   - 為何需要陣列（vs 個別宣告 100 個變數）
   - 陣列 vs 變數的使用時機

2. **陣列的三種初始化方式**
   ```
   方式 1：int[] nums = new int[5];           → 預設值填充
   方式 2：int[] nums = {1, 2, 3, 4, 5};     → 初始值清單
   方式 3：int[] nums; nums = new int[]{...}; → 先宣告後賦值
   ```

3. **各型別的預設值**

   | 型別 | 預設值 |
   |------|--------|
   | `int` | `0` |
   | `double` | `0.0` |
   | `boolean` | `false` |
   | `String`/Object | `null` |

4. **`length` 屬性**
   - 陣列用 `arr.length`（屬性，無括號）
   - 與 `String.length()`（方法，有括號）的區別
   - 最後一個元素的通用寫法：`arr[arr.length - 1]`

5. **索引（Index）概念**
   - 0-based indexing
   - 有效索引範圍：`0` 到 `arr.length - 1`
   - `ArrayIndexOutOfBoundsException` 的觸發條件

6. **程式碼範例（2 個）**
   - 建立並修改字串陣列
   - 通用存取最後一個元素

7. **練習題**
   - Easy：宣告陣列，印出首元素、末元素、長度
   - Easy：迴圈填入偶數 2, 4, 6, 8, 10

8. **常見錯誤表格（4 種）**

9. **現在試試看**
   - 建立 5 門課成績陣列，印出索引與成績對照

---

### Unit 12：Array 元素存取與修改

**預估時間：** 2 小時｜**難度：** ⭐⭐☆☆☆

#### 大綱

1. **概念導入**
   - 讀取元素（`arr[i]`）
   - 修改元素（`arr[i] = value`）
   - 陣列大小固定：無法新增或刪除元素

2. **參考型別（Reference Type）概念**
   - Java 的值型別 vs 參考型別
   - 陣列是參考型別：變數儲存記憶體位址
   - 傳入方法時的行為（Pass by Reference Effect）

3. **參考型別的影響**
   ```java
   int[] alias = original;  // ❌ 同一個陣列
   int[] copy = original.clone();  // ✅ 獨立副本
   ```
   - 陷阱：`=` 賦值不是複製
   - 方法內修改陣列 → 原始陣列也被改

4. **就地修改模式（In-place Modification）**
   - 批量加減乘除
   - 條件修改（將負數改為 0）
   - 元素交換（swap）

5. **程式碼範例（3 個）**
   - 存取與修改特定索引
   - 批量修改（將負數改 0）
   - 參考型別示範（方法修改後原陣列改變）

6. **練習題**
   - Easy：撰寫 `swapEnds()` 交換首尾元素
   - Medium：撰寫 `addOffset()` 將所有元素加上偏移量

7. **常見錯誤**
   - 誤以為賦值是複製
   - 忘記 `temp` 變數就地交換（直接覆蓋）

8. **現在試試看**
   - 撰寫 `reverse(int[] arr)` 原地反轉陣列

---

### Unit 13：Array 遍歷 — 標準 `for` 迴圈

**預估時間：** 2 小時｜**難度：** ⭐⭐☆☆☆

#### 大綱

1. **概念導入**
   - 陣列遍歷（Array Traversal）：逐一存取每個元素
   - 標準 for 迴圈的索引優勢
   - 遍歷的四大用途：印出、計算、搜尋、修改

2. **標準遍歷模式**
   ```java
   for (int i = 0; i < arr.length; i++) {
       // 使用 arr[i]
   }
   ```

3. **四大遍歷應用模式**

   | 模式 | 初始化 | 核心操作 |
   |------|--------|---------|
   | 印出所有元素 | — | `System.out.println(arr[i])` |
   | 累加 / 平均 | `sum = 0` | `sum += arr[i]` |
   | 計數（符合條件）| `count = 0` | `if (...) count++` |
   | 找最大值 | `max = arr[0]` | `if (arr[i] > max) max = arr[i]` |

4. **索引的特殊用法**
   - 從索引 1 開始（最大值初始化後）
   - 相鄰元素比較（`arr[i]` vs `arr[i+1]`）
   - 反向遍歷（從 `arr.length - 1` 到 0）

5. **程式碼範例（4 個）**
   - 印出所有元素（含索引）
   - 計算總和與平均
   - 找最大值
   - 計算 80 分以上人數

6. **練習題**
   - Easy：撰寫 `min(int[] arr)` 方法
   - Hard：撰寫 `hasDuplicate(int[] arr)` 使用巢狀迴圈

7. **現在試試看**
   - 找最大值的索引（回傳索引不是值，多個最大值取第一個）

---

### Unit 14：Array 遍歷 — 增強式 `for` 迴圈（for-each）

**預估時間：** 2 小時｜**難度：** ⭐⭐☆☆☆

#### 大綱

1. **概念導入**
   - 增強式 for 迴圈（Enhanced for loop / for-each）
   - Java 5 引入，語法更簡潔
   - 適用於：只需讀取元素，不需索引

2. **語法結構**
   ```java
   for (型別 變數 : 陣列) {
       // 使用 變數
   }
   ```
   - `型別`：與陣列元素型別一致
   - `變數`：本次迭代的元素副本
   - `陣列`：要遍歷的陣列

3. **for-each vs 標準 for 完整比較**

   | 比較項目 | 標準 for | for-each |
   |---------|---------|---------|
   | 取得索引 | ✅ | ❌ |
   | 修改元素 | ✅ | ❌（修改變數不影響陣列）|
   | 逆向遍歷 | ✅ | ❌ |
   | 語法複雜度 | 較高 | 較低 |
   | 適用場景 | 需索引/修改 | 純讀取 |

4. **for-each 修改失效的原因**
   - 迭代變數是元素的**副本**（primitive type）
   - 修改副本不影響原始陣列
   - 物件陣列的行為（物件本身可被修改）

5. **程式碼範例（4 個）**
   - for-each 印出整數陣列
   - for-each 計算總和
   - for-each 修改（錯誤示範 + 說明）
   - String 陣列 for-each

6. **練習題**
   - Easy：用 for-each 找最大值
   - Easy：用 for-each 判斷所有元素是否為正數

7. **現在試試看**
   - 計算 `double[]` 陣列所有元素的乘積（for-each）

---

### Unit 15：Array 演算法：最大值、最小值、總和、搜尋

**預估時間：** 2 小時｜**難度：** ⭐⭐⭐☆☆

#### 大綱

1. **概念導入**
   - 4 大基礎陣列演算法（APCS 必考）
   - 演算法（Algorithm）的概念：有限步驟解決問題
   - 選擇正確的初始值是關鍵

2. **最大值演算法（Maximum）**
   - 初始化：`max = arr[0]`（不能初始化為 0！）
   - 逐一比較，更新 `max`
   - 進階：同時記錄最大值的索引

3. **最小值演算法（Minimum）**
   - 初始化：`min = arr[0]`
   - 逐一比較，更新 `min`

4. **總和與平均（Sum & Average）**
   - 累加器初始化為 0
   - 注意整數除法：轉型為 `double`
   - `(double) sum / arr.length`

5. **線性搜尋（Linear Search）**
   - 逐一比較目標值
   - 找到後記錄索引（初始化為 -1 表示未找到）
   - 使用 `break` 提早終止
   - 最差情況時間複雜度：O(n)

6. **程式碼範例（4 個完整演算法）**

7. **練習題**
   - Medium：撰寫 `indexOfMax(int[] arr)` 回傳最大值索引
   - Hard：撰寫 `countAboveAverage(int[] arr)` 統計高於平均的個數

8. **演算法選擇指南**
   - 何時用最大/最小：統計類問題
   - 何時用線性搜尋：有無某元素、找特定值
   - APCS 考試中不需要二分搜尋（sorted array 有可能出現）

9. **現在試試看**
   - 撰寫方法回傳陣列中第二大的值

---
